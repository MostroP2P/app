include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    # cargokit is a standalone Dart build tool with its own pubspec and deps.
    # It is NOT part of the Flutter project and must not be analyzed.
    - cargokit/**

# Additional information about this file can be found at
# https://dart.dev/guides/language/analysis-options
